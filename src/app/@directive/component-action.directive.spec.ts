import { ComponentActionDirective } from './component-action.directive';

describe('ComponentActionDirective', () => {
  it('should create an instance', () => {
    const directive = new ComponentActionDirective();
    expect(directive).toBeTruthy();
  });
});
