/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { PresentationRoutingService } from './presentation-routing.service';

describe('Service: PresentationRouting', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [PresentationRoutingService]
    });
  });

  it('should ...', inject([PresentationRoutingService], (service: PresentationRoutingService) => {
    expect(service).toBeTruthy();
  }));
});
